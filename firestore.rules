rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all access
    // Access is granted through Cloud Functions (admin SDK bypasses rules)
    // Add specific rules as needed for client-side access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
